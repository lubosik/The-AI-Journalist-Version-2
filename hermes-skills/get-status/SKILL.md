---
name: herald-get-status
description: Report HERALD system and database status, active edition, publication timing, content volume, published editions, voice hooks, and Dom preferences.
---

Run:

```bash
cd /root/herald-v2 && python3 herald_cli.py status
```

Summarise the JSON clearly. Surface errors or suspicious zero counts directly.

