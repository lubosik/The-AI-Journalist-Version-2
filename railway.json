{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export CHAINLIT_AUTH_SECRET=${CHAINLIT_AUTH_SECRET:-$(python -c \"import secrets; print(secrets.token_hex(32))\")} && chainlit run app.py --host 0.0.0.0 --port $PORT --headless",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
